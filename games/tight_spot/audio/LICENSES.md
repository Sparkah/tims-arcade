# Tight Spot Audio Licenses

## bg_loop.mp3 — "Quiet Parking Grid"

- Source: generated by hand in the Suno web UI (suno.com) on 2026-07-29, instrumental.
- License: Suno Pro. Tracks generated while subscribed to Suno Pro/Premier carry
  commercial-use rights; free-tier tracks do NOT.
- Source master: `Quiet Parking Grid.wav`, sha256
  `edc9b257da672b0fd7bba5d4ec1acfb462948a023000e9d4a8ba5ca622eb0d44` (174.8s),
  archived at
  `output/mcplay-rejection-2026-07-30/music/tight-spot/Quiet Parking Grid.wav`.
- Local processing: ffmpeg trim + crossfade to a seamless 40.000s loop
  (`atrim=start=4:end=44`, 2s self-crossfade via `amix`), encoded with
  `libmp3lame -b:a 96k`, 48 kHz stereo -> 480,821 bytes, sha256
  `98759db657b2b93ae9ac2511fe493ff9520323324eecca298fc5255fc3e2d206`.
  Re-encoded 2026-07-30 from the WAV master (never transcoded from the earlier
  mp3) to clear the Playables validator's 512 KiB per-file target.
- No suno.com song-page URL was captured for this take, and it cannot be
  recovered from the local files. Tim is supplying it separately — do NOT invent
  one; platform moderation (Yandex 4.7 / 1.3) can ask, and a wrong URL is worse
  than an absent one. Capture the URL at generation time on the next import.
- Full provenance, including the generation prompt: `audio_manifest.json` and
  `output/mcplay-rejection-2026-07-30/music/tight-spot/suno_manifest.json`.

Playback is Web Audio only (`GF.music` -> `fetch` -> `decodeAudioData` -> looping
`AudioBufferSourceNode`). Never `new Audio()` / `<audio>` / `<video>`: those hand
the track to the OS media player, which is Yandex 1.6.2.5 / 1.6.1.6 and the root
of 1.3 + 4.7. This is the game's ONLY music bed — the procedural synth fallback is
deliberately unreachable (see the comment above the soundtrack bootstrap in
`index.html` and the `procedural music bed` gate in `build_youtube.py`).

SFX are synthesized in Web Audio by `gf-lib.js`; no sample files are involved.

## Superseded, deleted 2026-07-29

- `bg_track.mp3` — the original custom/procedural-replacement bed. Removed.
- `bg_track_elevator.mp3` — "Elevator Music" by Pro Sensory / Alex McCulloch,
  https://opengameart.org/content/elevator-music, CC0 / public domain. Removed
  when "Quiet Parking Grid" replaced it. Nothing references either file; the CC0
  attribution is kept here only so the history of the shipped bytes stays auditable.
