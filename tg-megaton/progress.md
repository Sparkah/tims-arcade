Original prompt: Work on Megaton Telegram infrastructure/integrations while Claude continues platform-agnostic game work. Add DB saving, Telegram SDK adapter, ads/shop/payment hooks, and keep the interface contract clear for the other agent.

2026-06-25:
- Added Cloudflare Pages Functions for Supabase-backed Telegram state and purchase receipt sync.
- Added `window.__tg` adapter surface in the Telegram wrapper: buy, showAd, saveState, loadState, getProducts.
- Bot payment receipts now post to the Cloudflare purchase endpoint when `TG_BACKEND_SECRET` is configured.
- Verified locally with Wrangler Pages dev and Playwright: wrapper boots the game, iframe receives `window.__tg`, shop renders all four Stars buttons on mobile, and console/page errors were empty.
- TON Stars and TON Connect lanes are both wired. Tim confirmed the real TON wallet purchase path worked on 2026-06-26.
- Next: test a real paid Stars invoice and rewarded-ad fill inside Telegram.
- Added AdsGram form values, Monetag zone wiring, `?adsgram=<blockId>` iframe forwarding, AdsGram-to-Monetag rewarded fallback, and the static TonConnect manifest.
- Set AdsGram production reward UnitID/block ID `36240` in `config.js`; keep `?adsgram=<blockId>` as a test override for alternate blocks.
- Set Monetag SDK source to `https://libtl.com/sdk.js`. Current game code uses Monetag Rewarded Interstitial (`show_11200728()`) and In-app Interstitial (`show_11200728({ type: "inApp", ... })`); Rewarded Popup is not active unless the call changes to `show_11200728("pop")`.
- Added TonConnect UI checkout in the Telegram wrapper, `/api/tg-ton-order`, `/api/tg-ton-verify`, TonAPI recipient-wallet verification, TON prices beside Stars, and the 20 TON `god_power` bundle. `god_power` persists as ad-free/max-warhead state and grants 250,000 caps.
- Added a context-only `welcome_x8` product: the welcome-back reactor modal now offers normal collect, x2 via rewarded ad/God Power, or x8 after a 10 Stars invoice returns `paid`.
- Production cleanup: removed the `?` help button and `RST` reset button from Megaton, moved Daily/Stats/Mute into a right rail below the top HUD, enabled first-60s funnel collection on `/tg-megaton/`, and added first-party level-funnel start/fail/complete events for city drop-off analysis under slug `megaton`.
- Fixed rewarded-ad routing for Telegram: the game iframe now asks the top Mini App wrapper to show rewarded ads first. The wrapper loads AdsGram + Monetag at top level, tries AdsGram block `36240`, falls back to Monetag zone `11200728`, and shows an "Ad not ready" toast on no-fill/missing SDK without granting fake rewards.
- Production phone pass: reduced interstitial cadence to ~1 per 5 minutes, recentered the city/action anchor for tall Telegram phone viewports, and ported the missing nuke selector from `Games/211_megaton` into the Telegram copy (Standard/Wide/Tsar, limited charges, preview/mushroom scaling, save-state persistence, God Power unlimited specials).
- Added `SKINS_GACHA_PLAN.md` with the backend schema, product IDs, 100-skin catalog shape, gacha odds, leaderboard counters, and implementation order for the next monetization/retention layer.
- Catch-up from Tim on 2026-06-26: gacha should be broad colorful collectibles, not only missile skins. Items can be missiles, mobs, mascots, drones, satellites, vehicles, anomaly payloads, or explosion styles. They should pop against the green Pip-Boy game, change the visible falling payload/trail/impact/explosion, and apply one small capped equipped stat bonus.
- Launched art-director worker and saved the direction in `ART_DIRECTOR_GACHA.md`; generated first reference sheet at `art_refs/gacha_collectibles_concept_01.png`.
- Local adaptation pass completed, not pushed/deployed: wrapper Arsenal now renders a 100-item colorful collectible catalog (payloads, drones, reactors, mobs/mascots, oddities), daily/caps/premium crate UI, local owned/equipped state, and iframe sync.
- Game iframe now preserves equipped collectible visual fields and applies them to falling payload silhouettes, trails, shockwave colors, fireball tint, mushroom-cloud palette, and impact variants such as cool rings, candy fission, fireworks, slime splats, grin clouds, and warning halos.
- Validation on local server `http://127.0.0.1:8899/tg-megaton/`: inline scripts parsed, develop-web-game Playwright smoke reached BLAST state with no errors, wrapper Playwright pass opened Arsenal, pulled a daily crate, confirmed equipped visual sync into iframe, forced Splitter Pod, captured falling payload/explosion screenshots, and passed non-green color pixel check.
- Local-only fix pass after Tim's screenshot: ported the prior daily-claim tutorial foreground fix from `Games/211_megaton` back into `Gallery/tg-megaton/game/index.html`. The daily crate is now redrawn above the tutorial scrim, highlighted amber, the hand sits below it, and the tutorial layer draws after CRT. Added `__gfTutorialDaily()` test hook. Verified with Playwright: `tutTarget: daily`, bright foreground pixels present, tap opens `dailyOpen: true`, no browser errors. Screenshot: `output/tg-megaton-tutorial-daily-fix/daily-tutorial-fixed.png`.
- Gemini GK art pass: used the temp Gemini API key path (`gk` equivalent) with `gemini-3.1-flash-image` and the art-direction reference `art_refs/gacha_collectibles_concept_01.png` to generate six 1024x1024 2x2 Arsenal sprite sheets under `art_pipeline/gemini_gk/sheets/`. Sliced 24 icons into `game/assets/gacha/icons/`, alpha-cleaned copies into `game/assets/gacha/icons_alpha/`, and wrote `game/assets/gacha/icon_manifest.json`.
- Arsenal UI now uses generated PNG icon assets for all 100 catalog entries via base `assetId` reuse, with procedural CSS art only as fallback. Added a small polish pass: alpha icon wells, image outlines, crisper font smoothing, roomier cards, specified transitions, and text-fit checks. Verified 100 cards render, no text overflow at 450px, no browser errors. Screenshot: `output/tg-megaton-gacha-art/arsenal-ui-polished.png`.
- Game iframe now preloads the alpha icon for the equipped collectible and draws it as the falling payload during the core loop. Verified `mythic_last_button` falling sprite screenshot at `output/tg-megaton-gacha-art/drop-mythic-last-button.png`, wrapper-to-iframe equip updates (`legendary_sun_crown`), and impact signature screenshot at `output/tg-megaton-gacha-art/impact-mythic-last-button-fixed.png`.
- Fixed a visible explosion tint square: removed full-frame `source-atop` tinting from `drawFireballs()` and kept the circular additive tint glow so alternate explosion colors do not reveal the sprite atlas frame.
- Paid Arsenal boxes are wired locally: `premium_1` now buys hidden Stars SKU `arsenal_payload` (25 Stars), `premium_10` buys `arsenal_payload_10` (199 Stars), successful paid callbacks open the box for real, grant/equip the best pulled collectible, sync it into the game iframe, and save `lastPaidProduct`. Added both SKUs to `functions/_lib/tgProducts.js`. Verified with fake Telegram invoice/purchase routes: the real `25 Stars` button equipped `common_coolant_cell_044` and rendered its generated icon during BLAST (`output/tg-megaton-purchase-skins/drop-with-real-click-paid-bomb.png`); the real `199 Stars` button revealed 10 cards, included rare pulls, equipped `rare_chaff_bloom_018`, and synced iframe asset `rare_chaff_bloom`.
- Corrected Arsenal gameplay art direction after Tim's note: all 24 base gacha icon assets were regenerated locally as vertical falling payloads using `art_pipeline/gemini_gk/regenerate_vertical_payloads.py`. Every sprite now uses the original warhead read: tail/flame/fins at the top, pointed nose/head at the bottom; mobs/mascots/slime/buttons are decals on one connected rocket body, not separate falling objects. Added `VERTICAL_PAYLOAD_SPEC.md`, updated `icon_manifest.json` with `tail_top_nose_bottom`, and removed the fixed diagonal icon rotation in `game/index.html`. Verified icon bounds programmatically, Arsenal screenshot `output/tg-megaton-vertical-payloads/arsenal-vertical-payloads.png`, and BLAST screenshot `output/tg-megaton-vertical-payloads/drop-barrel-imp-vertical-midair.png`.
- Local merge pass: removed the separate Arsenal/Shop split and made the bottom `SHOP` button open one merged Shop panel. Boxes stay first (daily/caps/premium/premium x10), then Starter Cache, Caps Pack, and God Power. Hidden Warhead Tuning/MIRV SKUs remain callable for old receipts but are no longer shown. Starter now grants 1,500 caps + 1.5x current priciest upgrade, +1 Yield, +2 Luck; Caps Pack grants 5,000 caps + 5x current priciest upgrade; God Power grants 250,000 caps + 10x current priciest upgrade before maxing perks. Updated wrapper fallback and `functions/_lib/tgProducts.js` descriptions/delivery tags. Verified fresh-save grants: Starter 1,583 caps, Caps Pack 5,275 caps, God Power 250,550 caps on tier 0.
- Fixed login bonus and daily reward modal layout after Tim's desktop screenshot: welcome-back reward text now reserves space above the x8/x2/collect stack instead of using a fixed `ph * 0.64` baseline, and daily claim/x2 buttons use the same fitted canvas button helper. Verified welcome and daily screenshots at 766x482, 390x844, and 320x568 under `output/tg-megaton-login-bonus/`; the `+6 caps` line no longer sits under the x8 button.
- Gemini art pass after Tim's pushback: used the `gk` temp Gemini API key path directly against the official Interactions image endpoint (`gemini-3.1-flash-image`) with one sprite per call, not 2x2 sheets, because sheet generation flipped several payloads. Added `art_pipeline/gemini_gk/generate_gemini_vertical_payloads.py`, staged raw JPEGs/processed PNGs under `vertical_single_*`, and installed all 24 higher-quality Gemini payload PNGs into `game/assets/gacha/icons*`. Regenerated the weak/flipped cells (`rare_sky_needle`, `rare_orbital_bolt`, `epic_hazard_mascot`, `legendary_prism_fission`, `common_bottlecap_buddy`) with stricter no-diagonal/no-upside-down prompts. Review sheet: `art_pipeline/gemini_gk/gemini_vertical_payload_sheet.png`; browser screenshots: `output/tg-megaton-gemini-vertical/collection-gemini-payloads.png` and `drop-gemini-last-button.png`.
- Gemini animation experiment: added `art_pipeline/gemini_gk/generate_gemini_animation_strips.py`, generated five 4-frame falling payload strips (`common_bottlecap_buddy`, `common_barrel_imp`, `rare_glow_slime`, `epic_hazard_mascot`, `mythic_last_button`), staged GIF previews/review sheet under `art_pipeline/gemini_gk/animation_experiments/`, added a local review page at `art_pipeline/gemini_gk/animation_experiments/index.html`, and installed their transparent frames into `game/assets/gacha/anim/`. The game now cycles those frames for equipped animated IDs during BLAST and falls back to the static Gemini icon for all other collectibles. Verified 20 frames are 128x128/non-empty, inline scripts parse, HTTP assets return 200, and a Playwright BLAST pass with `common_bottlecap_buddy` had no console errors. Screenshots: `output/tg-megaton-gemini-animations/bottlecap-fall-frame-a.png` and `bottlecap-fall-frame-b.png`.
- Full Gemini gacha juice pass: expanded `generate_gemini_animation_strips.py` to parse the real 100-item Arsenal catalog, use base payload art as a reference, generate variant-specific four-frame falling loops, install frames into `game/assets/gacha/anim/`, install GIF previews into `game/assets/gacha/anim_gifs/`, and write `game/assets/gacha/anim_manifest.json`. Reprocessed all raw Gemini sheets with edge-connected black/white background removal after several cells came back with white backgrounds. Final validation: 100 animated IDs, 400 installed PNG frames, 100 GIF previews, no large white-background frame failures, and no duplicate/static four-frame strips. Review page: `art_pipeline/gemini_gk/animation_experiments/index.html`; review sheet: `animation_review_sheet.png`.
- Gemini shop visual pass: added `generate_gemini_shop_visuals.py` and generated 7 UI assets for boxes/products: Daily Drop, Caps Crate, Premium Payload, Premium x10, Starter Cache, Caps Pack, and God Power. Installed to `game/assets/gacha/ui/boxes/` and `game/assets/gacha/ui/products/`, then rewired the merged Shop UI to render generated PNG art in crate/product cards and animated GIF previews in all collection cards. Runtime now requests exact equipped catalog animation IDs first (e.g. `common_coolant_cell_044`) and falls back to base static icons only if needed. Verified wrapper: 4/4 box PNGs load, 3/3 product PNGs load, 100/100 collection GIF sources render, no gacha asset 404s, no mobile shop text overflow. Verified gameplay: `common_coolant_cell_044` loads all four 128x128 frames and falls via the real `drop()` path. Screenshots: `output/tg-megaton-gemini-100/shop-generated-boxes-products-final.png`, `shop-mobile-generated-art.png`, `collection-animated-gifs.png`, and `drop-suffixed-coolant-044-real-fall.png`.
- Gemini animation cleanup after Tim flagged over-shaking and white edge residue: tightened `generate_gemini_animation_strips.py` with stronger no-body-motion prompts, shared per-strip cropping, largest-component frame anchoring, and edge-connected white/off-white/gray cleanup. Generated 100 additional animations into `art_pipeline/gemini_gk/animation_experiments_v2/`, then added `select_gemini_animation_best.py` to score v1/v2 candidates and install a best-of active set. Selected 54 v1 + 46 v2 strips into `art_pipeline/gemini_gk/animation_experiments_selected/`; the two named shaky items (`common_bottlecap_buddy_014`, `rare_triwing_courier_023`) now use v2. Active runtime remains 100 animated IDs, 400 installed PNG frames, and 100 GIF previews. Validation: zero edge-light frames, zero broken gacha assets in wrapper collection, 100/100 collection GIF sources load, and close-up contact sheet for the named items is stable. Review page: `art_pipeline/gemini_gk/animation_experiments_selected/index.html`; screenshots: `output/tg-megaton-selected-animations-smoke/collection-selected-animations.png` and `named-jitter-contact.png`.
- Stable import pass after Tim noticed some loops jumping vertically: upgraded `select_gemini_animation_best.py` to score body-only anchors (ignoring top flame/sparks), prefer the best v1/v2 candidate by body stability, re-anchor retained loops to a fixed body center, and automatically freeze any remaining unstable candidates to a clean single-frame loop. Reinstalled the active game set as 84 anchored animations + 16 static loops. Also strips low-saturation edge-connected leftovers and thin edge guide lines before writing game assets. Game draw path now removes the old procedural wobble from imported sprite images while leaving the trail effect intact, so generated payload bodies stay upright in BLAST. Validation: 100 items, 400 frames, 100 GIFs, max body drift 1px x/1px y, zero edge-component flags, zero edge-light frames, 100/100 collection GIFs load, and real `common_scrap_courier_043` drop reached BLAST with the imported sprite visible. Review page: `art_pipeline/gemini_gk/animation_experiments_selected/index.html`; screenshots: `output/tg-megaton-stable-animations-smoke/collection-stable-animations.png`, `drop-stable-scrap-courier.png`, and `output/tg-megaton-selected-animations-smoke/stable-contact-final.png`.
- Rejected corruption/uniqueness attempt: Tim correctly flagged that the prior alpha-hole fix still left visible corrupt cutouts and that the deterministic tint/decal "variant identity" pass looked bad. Do not reintroduce colour/decal overlays as a uniqueness shortcut.
- Shop economy update: Caps Crate now starts at 750 caps and doubles after each successful caps-open; wrapper charges through the game iframe via new `window.__gfSpendCaps()` so money cannot be overwritten by the iframe save loop. Premium Payload (`premium_1` / `arsenal_payload`) now guarantees rare+. Starter Cache now displays the computed total caps amount directly (no visible `+1.5x priciest upgrade` formula), grants +1 Yield, +2 Luck, and opens one included Premium Payload. Product descriptions in `functions/_lib/tgProducts.js` were updated to match. Validation with a fake Telegram invoice: Premium Payload returned `epic_bubblegum_core` (rare+), Starter granted 1,583 caps, +1 Yield, +2 Luck, and `rare_orbital_bolt_013`; Caps Crate test charged 750 caps and updated the next price to 1,500 caps.
- Rembg/OpenCV cutter rebuild after Tim's cutout complaint: added `art_pipeline/gemini_gk/recut_gemini_animation_sources.py`. It recuts raw Gemini JPG sheets with local `rembg` (`u2netp`) per cell, falls back to an OpenCV source-colour foreground mask for low-alpha cells, and only removes small edge artifacts so white/gray missile interiors are preserved. Recut v1/v2/v3 source pools now validate at 0 weak frame sets.
- Removed the bad artificial colour differentiator from `select_gemini_animation_best.py`: no `IDENTITY_COLORS`, no tint/decal overlays, and no `identity` fields in `game/assets/gacha/anim_manifest.json`. Selection now penalizes near-duplicate source candidates by image signature and chooses real generated art instead.
- Generated a third local Gemini round (`animation_experiments_v3`) with stricter silhouette-uniqueness prompt language (variant geometry changes: fins, pods, bands, windows, spikes, armor, nose shape). Recut it through the new cutter and selected from v1/v2/v3. Final active selection uses 19 v1 + 28 v2 + 53 v3 strips, 66 static/stable + 34 anchored loops.
- Final local validation: `game/assets/gacha/anim_manifest.json` has 100 items, 400 PNG frames, 100 GIF previews, no missing assets, no `identity` field, and minimum frame alpha area 1674. Review page: `http://127.0.0.1:8899/tg-megaton/art_pipeline/gemini_gk/animation_experiments_selected/index.html`. Browser checks: selected review page loads 100 GIFs; collection seeded with `legendary_sun_crown_004`, `epic_bubblegum_core_013`, `rare_orbital_bolt_013`, and `common_smoke_loop_021` had 4 visible owned cards and 0 bad images; iframe reported `equippedSkin: legendary_sun_crown_004`; launch test reached destruction and captured mid-fall sprite. Screenshots under `output/tg-megaton-rembg-final-smoke/`.
- 2026-06-27 local shop/reward pass: mission rewards now queue hidden `Mission Crate` tickets in Shop instead of opening instantly; first friend share queues one Mission Crate, later friend shares are repeatable every hour and grant caps equal to `repeatCapsFactor` (0.1) times the current biggest upgrade cost. Added `Legendary Payload` as a 199 Stars / 1.60 TON `arsenal_legendary_payload` paid box with `legendary_plus` odds (90% legendary / 10% mythic), server product catalog entry, TON price, and paid crate report validation. Updated `data/sheet_template/*.csv`, `tools/import_economy_sheet.py`, and regenerated `data/economy-missions.js` so these values remain sheet-editable. Localhost tester now has Free Legendary, Reward Crate, Share Friend, and Ready Share controls. Validation: inline scripts + Node/Python checks passed; Playwright verified mission claim queues a crate in Shop, first friend share queues and consumes a pending crate, hourly share grants caps, and local Legendary Payload reveals legendary/mythic. Screenshots under `output/tg-megaton-share-crates-client/`.
- 2026-06-27 mobile strike performance pass: added a phone-sized FX profile in `game/index.html` for BLAST sequences. On mobile/coarse portrait viewports, repeated high-tier strike visuals now cap at 2 mushrooms, 10 fireball sprites, ~135 chunks, ~82 dust puffs, and shorter smoke/fireball lifetimes; per-frame collapse debris/dust/fire regeneration is reduced. Added short-lived colored impact pulses/rays so the hit still reads juicy without keeping many expensive sprites alive. Validation: inline scripts parse; Playwright high-tier God Power/Tsar mobile strike reached BLAST and 100% destruction with `fxMobile: true`, early caps `{chunks:135,dust:82,fires:16,smoke:4,fireballs:10,mushrooms:2,pulses:4}`, late fireballs/particles decayed to 0; bundled develop-web-game smoke passed with only the expected local SimpleHTTP `level-funnel` 501. Screenshots under `output/tg-megaton-mobile-fx/`.
- 2026-06-27 channel/analytics/web-build pass: updated the `follow_main_channel` mission to `https://t.me/gamefactorytech` in wrapper fallback config, `data/economy-missions.js`, and `data/sheet_template/missions.csv`. Added `marketing/gamefactory-avatar.png` as an upload-ready square channel avatar draft. Public web builds now hide Daily/Ad/Reward crates and redirect Stars/TON/free-reward attempts to `https://t.me/gamesfactorybot/megaton?startapp=gamefactory`; localhost retains the local crate tester. Integrated GameAnalytics JS SDK v4.4.6 in the Telegram wrapper with game key `28543ab05121fd7f4572508b5ff9c915`, Telegram numeric ID as GA user id when available, and progression events bridged from the game iframe (`Start`/`Fail`/`Complete` by `campaign/<zone>/level_NNN`). `saveMeta()` now notifies the Telegram wrapper so `/api/tg-state` keeps current/max city level fresher in Supabase. Validation: inline scripts parse, external economy config parses, and develop-web-game local smoke passed with only the expected shared Playwright module warning.
- 2026-06-28 desktop eye-view fix: added a dedicated `viewZoom` camera target for the eye/city inspection mode and bound desktop mouse-wheel zoom only while `cityView` is active. New cities reset `viewZoom` to 1. Validation: inline scripts parse; bundled develop-web-game smoke passed; targeted Playwright clicked the eye button, wheeled up, and confirmed `cityView: true` with zoom increasing from 1 to 2.23 and no unexpected console errors. Screenshot: `output/tg-megaton-eye-zoom-smoke/eye-zoom-after-wheel.png`.
- 2026-06-28 end-of-level FX fix: moved the BLAST -> RESULT transition to the end of the update frame and made it wait for blast aftermath queues (collapse/sink/downed targets, debris, fire/smoke, mushroom, impact pulses, and juice FX) to drain before showing the result panel. Fire spread no longer keeps creating new burn FX after active collapse motion ends, and non-scoring movers are removed as RESULT begins. Validation: inline scripts parse; bundled develop-web-game smoke passed; targeted Playwright detonation reached RESULT only after all tracked FX and juice counts were zero.
- 2026-06-28 local-only war-room map style prototype: after reverting the aim/fire rewrite, tested a less neon visual direction on the original central-atom interaction model. Shifted canvas chrome toward a physical briefing-map language: muted paper/ink palette, stamped panels, brass/red launch accents, octagonal perk tokens, flatter HUD/buttons/tutorial/result/help surfaces, and reduced CRT glow into subtle map grain. Not committed or deployed. Validation: inline scripts parse; diff check clean; bundled develop-web-game smoke passed; targeted desktop Playwright confirmed first-run tutorial targets central atom and atom tap advances to Yield step, city tap fires immediately into BLAST, RESULT appears after FX queues hit zero, no blast report exists, and eye-view wheel zoom still works (1 -> 2.18); targeted mobile 390x844 confirmed central atom fits and reaches RESULT with no unexpected browser errors. Screenshots: `output/tg-megaton-warroom-smoke-2/shot-0.png`, `output/tg-megaton-warroom-targeted/mobile-aim.png`, `output/tg-megaton-warroom-targeted-2/mobile-result.png`, `output/tg-megaton-warroom-targeted/desktop-result.png`. Consult: separate agent review recommended keeping the full aim/fire revert and not salvaging bottom NUKE/blast report/tutorial rewrite; Claude CLI no-tool/read-only consult was attempted twice but hung without output.
- 2026-06-28 smoke-tail/shop verification pass: accelerated only the late BLAST tail queues (`impactPulses`, mushroom, smoke, dust, fire) after `collapseMotionActive()` is false, so the main destruction still plays before the level ends but leftover haze clears faster. Local wrapper verification confirmed `SHOP` and `MISSIONS` are visible from first frame at `/tg-megaton/index.html`, and `SHOP` opens the merged Shop panel immediately; the direct `/tg-megaton/game/index.html` canvas entry intentionally has no wrapper shop button. Validation: inline scripts parse; diff check clean; bundled develop-web-game smoke passed; targeted Playwright reached RESULT with all FX/juice queues at zero in ~3267ms desktop and ~2519ms mobile. Screenshots: `output/tg-megaton-wrapper-shop-check/wrapper-shop-visible-final.png`, `output/tg-megaton-final-smoke/desktop-result.png`, and `output/tg-megaton-final-smoke/mobile-result.png`.
- 2026-06-28 local smoke-tail correction after Tim's feedback: the fast cleanup multiplier now waits until the main mushroom cloud is fully gone; mushroom clouds always advance at normal speed, and only residual smoke/dust/fire/impact queues accelerate after `mushrooms.length === 0` and core collapse motion is inactive. Validation: inline scripts parse; diff check clean; bundled develop-web-game smoke passed; targeted Playwright confirmed mid-blast stays `BLAST` while `mushrooms: 1`, then reaches RESULT with all FX queues zero (~5824ms desktop, ~3534ms mobile). Added Reddit-feedback backlog items in `Knowledge/Backlog.md`: gameplay-first rescue, landmark aiming, procedural explosion generator, aimed MIRV follow-up strikes, crit/luck redesign, world map layer, and caps/UI readability.
- 2026-06-28 local mushroom-speed correction: Tim clarified the initial explosion is fine, but the rising/fading mushroom cloud itself is too slow. Kept the first hot fireball timing unchanged (`t < 0.7` still uses real time), then doubled the mushroom cloud visual clock after that point and shortened mushroom lifetime to match (~2.75s desktop, ~1.7s mobile). Validation: wrapper Playwright check confirmed `SHOP` visible while the iframe detonates; hot phase has active fireballs/mushroom, cloud phase stays BLAST, RESULT waits for all FX queues to hit zero (~4375ms desktop, ~2694ms mobile). Test/handoff should use wrapper URL only because Shop is part of the product surface.
- 2026-06-28 local mushroom-speed variant: changed the post-hot-fireball mushroom cloud clock from 2x to 2.5x and matched mushroom lifetime to ~2.35s desktop / ~1.45s mobile. Initial explosion timing remains unchanged.
- 2026-06-29 local Extra Income balance pass: renamed player-facing Luck to Extra Income while keeping the `luckLvl` save/purchase field for compatibility. Removed critical/fizzle blast-radius RNG from detonation, tightened normal yield variance to 0.92-1.08, and made the former Luck path a capped caps-income multiplier plus occasional capped `CRITICAL PAYOUT` money bonus only. Yield upgrades were nudged up (`CELLS_PER_LVL` 0.60 -> 0.66). Wrapper/shop/product docs now say Extra Income instead of Luck/Critical chance, reset-run now clears the old `luckLvl` field, the stale RU help tip was rewritten, and the old clover fallback icon is now a caps-token icon. Validation so far: diff check clean; wrapper/game inline scripts parse; `functions/_lib/tgProducts.js` parses.
- 2026-06-29 local procedural explosion pass: added a seeded `explosionProfile()` per blast so each detonation varies fire atlas, heat/smoke tint, fireball scale/lifetime, mushroom lean/lift/cap squash/billow count, shock ring squash/rotation, smoke-column count, secondary fireballs, and dust-ring density. The first hot flash timing remains unchanged; the post-hot mushroom clock stays at the requested 2.5x. Added `lastBlastProfile` to `__gfDbg()` plus a BLAST-state test hook for wrapper-only validation. Validation: diff check clean; wrapper/game inline scripts parse; `functions/_lib/tgProducts.js` parses; bundled develop-web-game smoke passed; targeted wrapper Playwright passed on mobile 430x860 and desktop 960x720 with `SHOP` visible, old Luck/Critical chance wording absent, different profile seeds/summaries across two same-tier blasts, active mushroom/fireball FX, and RESULT reached after the blast lifecycle. Screenshots: `output/tg-megaton-procedural-targeted-mobile/run1-midblast.png`, `run2-midblast.png`, `output/tg-megaton-procedural-targeted-desktop/run1-midblast.png`, and `run2-midblast.png`.
- 2026-06-29 local shockwave/blob explosion pass: pushed the procedural FX further after Tim said the first version was only a tiny bit better. Normal blast waves now draw a short-lived over-city pressure front with seeded streak arcs, while the update loop kicks more bounded dust along the wave edge. The mushroom renderer now uses seeded dark smoke, hot inner lobes, turbulent stem blobs, and ground-hugging smoke instead of one smooth cap. Kept mobile/desktop caps and the existing BLAST -> RESULT lifecycle. Validation: diff check clean; wrapper/game inline scripts parse; bundled develop-web-game smoke passed; targeted wrapper Playwright passed on mobile 430x860 and desktop 960x720 with `SHOP` visible, active wave/mushroom FX, and RESULT reached. Reviewer found no blockers but flagged multi-blast density risk, so wave-front arc density now scales down by active wave count and `__gfDbg().fx` exposes `waves` for validation. Mobile stress test at tier 13 with God Power/Tsar, MIRV 3, Cluster 5, Inferno 5, airport + refinery produced 22 concurrent waves max, stayed within mobile caps (`chunks:135`, `dust:84`, `fireballs:10`, `mushrooms:2`, `fires:16`), and reached RESULT with all tracked FX queues zero. Screenshots: `output/tg-megaton-shockblob-final-mobile/early-shockwave.png`, `blob-mushroom.png`, `output/tg-megaton-shockblob-final-desktop/early-shockwave.png`, `blob-mushroom.png`, and stress screenshots under `output/tg-megaton-shockblob-stress-mobile-refinery/`.
- 2026-06-29 local CC0 explosion-atlas texture pass: added OpenGameArt/Kenney-derived `fx/explosion_atlas_512x512.png` (CC0) and sample it as low-alpha additive, seed-selected texture stamps inside the procedural mushroom cap and ground cloud. This keeps the whole explosion profile procedural while adding more natural fire/fog breakup than pure canvas gradients. Credits updated in `game/fx/CREDITS.txt`.
- 2026-06-29 local atlas rollback/perf pass after Tim's feedback: removed the CC0 atlas from the mushroom because it still read as sprite-made and added first-hit decode/draw cost. The mushroom cloud is back to fully canvas-generated seeded geometry: dark cap/stem blobs, smoke flecks, hot flecks, and ground puffs are all procedural. Replaced the early fireball sprite-sheet renderer with seeded canvas gradients/lobes as well, so the blast no longer loads `fx/ex1-4.png` at runtime.
- 2026-06-29 local free-asset integration pass: downloaded Kenney's CC0 Smoke Particles pack, selected 10 explosion ingredients (6 fire blooms, 2 flashes, 2 smoke puffs), added them under `game/fx/free_explosion_candidates/`, recorded provenance in `ART_SOURCES.md` and `game/fx/CREDITS.txt`, and wired them into the procedural generator as low-alpha seeded fire/flash/smoke texture ingredients rather than a fixed animation.
- 2026-06-29 local nuke/MOAB asset scout: downloaded 10 CC0/public-domain mushroom/nuclear explosion candidates under `game/fx/nuke_mushroom_candidates/`, saved original sources under `_source_assets/explosion_candidates/nuke_mushroom/`, and added four public-domain MOAB dust-plume reference frames under `game/fx/moab_reference_candidates/`. Wrote license/provenance notes in `ART_SOURCES.md`; these new nuke/MOAB assets are not wired into runtime yet. Second-agent review recommended using the best mushroom images as masks/textures for a procedural stack, rejecting the silhouette/pictogram/garbage-text finds, and treating the MOAB frames as dust/shock reference only.
- 2026-06-29 local source-mask mushroom experiment: wired the five best FreeSVG mushroom/nuke assets into the runtime as offscreen alpha masks, not as static sprites. Each blast profile now picks two weighted mushroom source masks plus seeded warp/flip/rotation/alpha settings; `drawMushroom()` draws those masks through a per-frame tinted/noisy scratch canvas in horizontal warped slices, then keeps the existing procedural cap/stem/blob/smoke layers over it. `__gfDbg()` exposes `mushroomSources` and the chosen `mushroomMask` fields for validation. Not pushed yet; needs Tim visual judgment.
- 2026-06-29 source-mask iteration after visual review: broad sliced masks looked too pasted/rectangular, so the selected assets now generate sampled smoke/fire puffs from precomputed alpha points instead. Desktop keeps a faint slice texture under the puffs; mobile skips that expensive layer and uses cheaper flat puffs. Raised the low-level mushroom scale floor (`0.66 + powerLvl * 0.04`) so first strikes show the source-shaped cloud, dimmed/narrowed the old hot stem beam, and added a subtle base shock glow. Validation: inline scripts parse, diff check clean, bundled develop-web-game wrapper smoke passed, desktop wrapper captures show varied `mushroomMask` profiles with 5 loaded source assets, and mobile perf recovered to max 14.4ms / p95 11.7ms / 0 frames over 33ms. Known localhost-only console noise: SimpleHTTP 501 POSTs for backend endpoints.
- 2026-06-29 mushroom timing tweak after Tim's approval: changed `drawMushroom()` geometry so the cap/stem are near full size as soon as the mushroom appears (`plumeK = 0.9 + 0.12 * rise`, `liftK = 0.88 + 0.14 * rise`) and then only rise/expand lightly. Everything else in the source-mask/puff explosion direction stayed intact. Validation: inline scripts parse, diff check clean, bundled develop-web-game wrapper smoke passed, and second-agent visual review confirmed early frame reads full-size while late frame only drifts/spreads lightly.
- 2026-06-29 independent-chaos FX pass after Tim's feedback: kept the full-size mushroom timing intact but decoupled the supporting effects from the main mushroom transform. Source-mask puffs and faint sliced texture now use per-puff/per-slice lagged drift; smoke flecks, hot flecks, cap billows, detached cap wisps, ground smoke, and embers now use separate seeded motion clocks so the explosion does not read as one organized pattern. Validation: diff check clean; wrapper/game inline scripts parse; bundled develop-web-game wrapper smoke passed; targeted desktop captures show `SHOP` visible before and during blast; mobile frame-step sample stayed within budget (p95 ~0.2ms, max ~2.9ms, 0 frames over 33ms). Screenshots: `output/tg-megaton-independent-chaos-v2/desktop/early.png`, `mid.png`, `late.png`. Known localhost-only console noise remains SimpleHTTP 501 POSTs for backend endpoints.
- 2026-06-29 random-no-spiral correction after Tim's feedback: Tim could still see organized fog motion and specifically rejected spiraling/predictable patterns. Do not reintroduce evenly spaced angular fog placement or sine/cosine orbit drift for mushroom smoke. Replaced fog motion with seeded value-noise/random-walk offsets, randomized dust/smoke spawn angles, and source-mask puff morphing between random mask samples. Reduced the faint sliced asset layer so it cannot trace a recognizable curve. Validation: diff check clean; wrapper/game inline scripts parse; bundled develop-web-game wrapper smoke passed; desktop captures under `output/tg-megaton-random-no-spiral-v2/desktop/`; mobile frame-step sample stayed within budget (p95 ~0.1ms, max ~2.6ms, 0 frames over 33ms). Known localhost-only console noise remains SimpleHTTP 501 POSTs for backend endpoints.
- 2026-06-29 base-ring breakup follow-up: second-agent visual review said the spiral read was gone but faint amber base ovals still looked patterned. Converted normal shock/crater/impact pulse ellipses into short seeded broken arcs, randomized pulse rays, lowered base pulse opacity, and reduced the mushroom base glow. EMP/glass specialty rings remain intact because they intentionally communicate upgrade effects. Validation: diff check clean; wrapper/game inline scripts parse; final desktop captures under `output/tg-megaton-random-no-spiral-final/desktop/`; mobile frame-step sample stayed within budget (p95 ~0.1ms, max ~2.5ms, 0 frames over 33ms). Known localhost-only console noise remains SimpleHTTP 501 POSTs for backend endpoints.
- 2026-06-29 middle-smoke removal after Tim rejected the chaotic moving smoke: removed the full-body source-mask puff pass from the mushroom stem and disabled blast-center smoke columns (`smokeCols: 0`) so the middle no longer has jittering blob smoke. The remaining mushroom is cap/ground dust plus the calmer stem gradient. Validation: diff check clean; wrapper/game inline scripts parse; wrapper screenshots under `output/tg-megaton-no-middle-smoke/desktop/`; mobile frame-step sample stayed within budget (p95 ~0.1ms, max ~3.3ms, 0 frames over 33ms). Known localhost-only console noise remains SimpleHTTP 501 POSTs for backend endpoints.
- 2026-06-29 top-smoke stability pass after Tim's feedback: Tim liked the center-smoke removal but said the top cap smoke still looked like it was vibrating. Changed the cap source-mask puffs to stable seeded alpha points and removed time-noise offsets from the top cap's loose puffs, smoke flecks, hot flecks, and billows. These now expand/rise with per-puff speed differences instead of hopping between random positions. Validation: diff check clean; wrapper/game inline scripts parse; targeted wrapper captures under `output/tg-megaton-top-smoke-steady/desktop/`; mobile frame-step sample stayed within budget (p95 ~0.1ms, max ~2.4ms, 0 frames over 33ms). Known localhost-only console noise remains SimpleHTTP 501 POSTs; bundled web-game client also reports local wrapper ad timeout noise.
- 2026-06-29 bottom-cloud removal after Tim's feedback: Tim said the circular/oval vibrating clouds at the bottom were bad. Removed the mushroom ground-smoke blob loop, base glow oval, and base smoke texture stamp. Reworked `drawDust()` from round cloud puffs into small rectangular grit/debris, and reduced the main nuke juice burst count/size/lifetime so it does not create another bottom cloud. Validation: diff check clean; wrapper/game inline scripts parse; targeted wrapper captures under `output/tg-megaton-no-bottom-ovals-v3/desktop/`; mobile frame-step sample stayed within budget (p95 ~0.1ms, max ~1.8ms, 0 frames over 33ms). Known localhost-only console noise remains SimpleHTTP 501 POSTs; bundled web-game client also reports local wrapper ad timeout noise.
- 2026-06-29 rising-dot removal after Tim's feedback: removed the mushroom ember loop that drew yellow dots drifting upward, stripped the cap's hot-fleck/hot-billow dot layers, and changed source-mask cap puffs to smoke-toned texture instead of hot yellow puffs. Validation: diff check clean; wrapper/game inline scripts parse; targeted wrapper captures under `output/tg-megaton-no-rising-embers/desktop/` with `SHOP` visible; mobile frame-step sample stayed within budget (p95 ~0.1ms, max ~2.0ms, 0 frames over 33ms). Known localhost-only console noise remains SimpleHTTP 501 POSTs; bundled web-game client also reports local wrapper ad timeout noise.
